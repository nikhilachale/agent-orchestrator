---
title: Cursor
description: Cursor Agent CLI — IDE-like editing from the terminal, with PATH-wrapper PR tracking.
---

import { Accordions, Accordion } from "fumadocs-ui/components/accordion";

<div style={{ display: "flex", alignItems: "center", gap: "0.75rem", margin: "0.5rem 0 1.25rem" }}>
	<Logo name="cursor" size={28} />
	<span style={{ fontSize: "0.8125rem", color: "var(--color-fd-muted-foreground)" }}>
		Slot: <code>agent</code> · Name: <code>cursor</code> · Binary: <code>cursor-agent</code>
	</span>
</div>

The [Cursor Agent CLI](https://cursor.com/docs) brings Cursor's editing model to a terminal. AO spawns it per session, wraps `gh`/`git`, and tracks activity via the AO activity log.

<PlatformSupport macos="full" linux="full" windows="full" />

## Install

```bash
curl https://cursor.com/install -fsS | bash
```

Verify:

```bash
cursor-agent --help
```

You should see "Cursor Agent" in the output.

## Use

```yaml title="agent-orchestrator.yaml"
agent: cursor
```

No plugin-level config.

## How it works

- **Launch:** AO creates a Cursor chat with `cursor-agent create-chat`, stores the returned chat id, then runs `cursor-agent --resume <chatId> -- <prompt>` inside the worktree.
- **Activity tracking:** AO installs workspace-local Cursor hooks that report activity and can confirm the native chat id when Cursor emits it.
- **Session resume:** AO restores with `cursor-agent --resume <chatId>` when a captured chat id exists. Sessions without `agentSessionId` use AO's shared restore fallback: a fresh Cursor conversation with the recomputed system prompt and saved task prompt when available.

## Detection

The plugin resolves `cursor-agent` from PATH or common install locations.

## Environment variables

| Variable          | Set by AO | Purpose                                 |
| ----------------- | --------- | --------------------------------------- |
| `AO_SESSION_ID`   | ✓         | AO session id                           |
| `AO_ISSUE_ID`     | ✓         | Issue identifier                        |
| `AO_DATA_DIR`     | ✓         | AO data root for hooks                  |
| `CURSOR_DATA_DIR` | ✓         | Isolated Cursor profile under `~/.ao`   |

## Troubleshooting

<Accordions>
	<Accordion title="AO says Cursor Agent isn't detected">
		Run `cursor-agent --help`. If it is missing, reinstall Cursor Agent or add the install directory to PATH.
	</Accordion>
	<Accordion title="An older Cursor session starts a fresh conversation on restore">
		Only Cursor sessions created after AO stored a native chat id have deterministic transcript resume. Older sessions
		without `agentSessionId` fall back to a fresh Cursor conversation using AO's saved task prompt when one exists.
	</Accordion>
</Accordions>
